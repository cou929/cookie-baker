<!DOCTYPE html>
<html>
<body>
  <h1>Cleared Cookies</h1>

  <table border="1">

    <tr>
      <th>key</th>
      <th>length</th>
      <th>value</th>
    </tr>

    <% @cookies.each do |key, value| %>
    <tr>
      <td><%= key %></td>
      <td><%= (key + '=' + value).length  %></td>
      <td><%= value %></td>
    </tr>
    <% end %>

  </table>
</body>
</html>
